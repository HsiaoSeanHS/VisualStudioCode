name: Anki Daily Review

on:
  workflow_dispatch: # Manual trigger
  repository_dispatch: # API trigger
    types: [schedule_anki_review]
  schedule:
    - cron: '12 13 * * *' # Default schedule - 15:00 UTC (23:00 Taipei Time)

jobs:
  run_anki_review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install jupyter nbconvert

      - name: Run Anki Review Notebook
        env:
          ANKI_ACCOUNT: ${{ secrets.ANKI_ACCOUNT }}
          ANKI_PASSWORD: ${{ secrets.ANKI_PASSWORD }}
        run: |
          jupyter nbconvert --to notebook --execute --inplace an-ekphrasis.ipynb

      - name: Print execution log
        run: |
          jupyter nbconvert --to markdown an-ekphrasis.ipynb
          cat an-ekphrasis.md
